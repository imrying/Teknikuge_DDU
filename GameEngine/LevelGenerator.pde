class LevelGenerator {
  
  LevelGenerator() {
  }
}

class LevelPrefab {
  ArrayList<GameObject> gameObjects;

  LevelPrefab(ArrayList<GameObject> objects) {
    gameObjects = objects;
  }
}
